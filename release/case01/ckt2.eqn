# Equations for "top" written by ABC on Sat Dec 20 21:53:14 2025
INORDER = x0 x1 y0 y1 z;
OUTORDER = u0 u1 w0 w1;
u0 = y0 * x0;
u1 = (x1 * y0 * (!x0 + !y1)) + (x0 * y1 * (!x1 + !y0));
w0 = (y1 * (!y0 + !z)) + (y0 * z * !y1);
w1 = (!y0 * z) + (y0 * !z);

